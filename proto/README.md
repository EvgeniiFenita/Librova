# Proto

This folder is reserved for shared protobuf contracts between `LibriFlow.UI` and `LibriFlow.Core`.

Contracts are intentionally not introduced in the bootstrap step because the first checkpoint only establishes repository structure and build entry points.
